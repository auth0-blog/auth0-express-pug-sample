extends ../components/layout

block content
  div
    h1 Profile
    p
      | You use will use an ID Token to get the profile information of a logged-in user.
      br
      strong This route should be protected.
